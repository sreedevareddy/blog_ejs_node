<%- include('partials/header') -%>
    <h1>Home</h1>
    <p>
        <%= startingContent %>
    </p>
    <% for( let i = 0; i < posts.length; i++ ) { %>
        <a href="/posts/<%= posts[i].title %> " target="_blank" rel="noopener noreferrer"><h1 class="post"><%= posts[i].title %> </h1></a>
        <p class="post"><%= posts[i].content.substring(0, 100) + "........" %> </p>
        <br>
        <% console.log(posts[i].title) %> 
    <% } %>
<%- include('partials/footer') -%>